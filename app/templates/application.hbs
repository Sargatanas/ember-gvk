<div class="form">
    <div class="form__comment">
        Для теста рекомендуется использовать неделю 25.11.2019-1.12.2019 и номера бригад 101-102
        <button 
            class="form-header__element form-header__element_button form-header__element_small-button" 
            {{action "showDefaulData"}}>
                Заполнить по дефолту
        </button>
    </div>

    <header class="form-header">
        <label class="form-header__element" for="team-id">
            Номер бригады:
            <input id="add-team-id" oninput={{action "validateInputs"}} value={{inputTeamId}}>
        </label>
        {{input-error errors=errors.team}}
        
        <label 
            class="form-header__element" 
            for="add-date">
                Дата (в формате ДД.ММ.ГГГГ):
                <input id="add-date" oninput={{action "validateInputs"}} value={{inputDate}}>
                {{currentDateString}}
        </label>
        {{input-error errors=errors.date}}

        {{#if isShowButtons}}
            {{#if isTasksCreated}}
                <button 
                    class="form-header__element form-header__element_button"
                    {{action "showTable"}}>
                        Показать таблицу
                </button>
                {{form-task tasks=freeTaskList}}
            {{else}}
                <button 
                    class="form-header__element form-header__element_button"
                    {{action "createTasks"}}>
                        Генерировать список заявок
                </button>
            {{/if}}  
        {{/if}}              
    </header>
    {{#if isShowTable}}
        <main 
            class="form-body" 
            id="table">
                {{#each week as |day|}}
                    {{table-form hours=hours 
                                 day=day 
                                 date=date 
                                 status=status 
                                 taskList=taskList
                                 shiftOptions=shiftOptions
                                 totalTask=(getTotalTask (getDateForm (getDate day.id date.value)) taskCount)}}
                {{/each}}             
        </main>

        <footer class="form-footer">
            <h2 class="form-footer__title">
                Свободные заявки
            </h2>
            {{form-task taskClass="element-body-task_not-table" tasks=freeTaskList}}        
        </footer>
    {{/if}} 
</div>