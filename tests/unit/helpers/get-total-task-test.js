import { getTotalTask } from 'ember-gvk/helpers/get-total-task';
import { module, test } from 'qunit';

module('Unit | Helper | get total task');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getTotalTask([42]);
  assert.ok(result);
});
