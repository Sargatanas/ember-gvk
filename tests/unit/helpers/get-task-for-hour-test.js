import { getTaskForHour } from 'ember-gvk/helpers/get-task-for-hour';
import { module, test } from 'qunit';

module('Unit | Helper | get task for hour');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getTaskForHour([42]);
  assert.ok(result);
});
