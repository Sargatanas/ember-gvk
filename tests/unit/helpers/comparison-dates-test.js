import { comparisonDates } from 'ember-gvk/helpers/comparison-dates';
import { module, test } from 'qunit';

module('Unit | Helper | comparison dates');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = comparisonDates([42]);
  assert.ok(result);
});
