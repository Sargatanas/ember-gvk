import { getTaskHeight } from 'ember-gvk/helpers/get-task-height';
import { module, test } from 'qunit';

module('Unit | Helper | get task height');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getTaskHeight([42]);
  assert.ok(result);
});
