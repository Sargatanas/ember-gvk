import { getShiftBorders } from 'ember-gvk/helpers/get-shift-borders';
import { module, test } from 'qunit';

module('Unit | Helper | get shift borders');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getShiftBorders([42]);
  assert.ok(result);
});
