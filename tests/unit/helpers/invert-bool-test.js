import { invertBool } from 'ember-gvk/helpers/invert-bool';
import { module, test } from 'qunit';

module('Unit | Helper | invert bool');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = invertBool([42]);
  assert.ok(result);
});
