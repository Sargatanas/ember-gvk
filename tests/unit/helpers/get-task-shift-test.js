import { getTaskShift } from 'ember-gvk/helpers/get-task-shift';
import { module, test } from 'qunit';

module('Unit | Helper | get task shift');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getTaskShift([42]);
  assert.ok(result);
});
