import { isTotalCountEmpty } from 'ember-gvk/helpers/is-total-count-empty';
import { module, test } from 'qunit';

module('Unit | Helper | is total count empty');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isTotalCountEmpty([42]);
  assert.ok(result);
});
