import { getZIndex } from 'ember-gvk/helpers/get-zindex';
import { module, test } from 'qunit';

module('Unit | Helper | get zindex');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getZIndex([42]);
  assert.ok(result);
});
