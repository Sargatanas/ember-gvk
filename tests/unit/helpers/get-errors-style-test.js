import { getErrorsStyle } from 'ember-gvk/helpers/get-errors-style';
import { module, test } from 'qunit';

module('Unit | Helper | get errors style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getErrorsStyle([42]);
  assert.ok(result);
});
