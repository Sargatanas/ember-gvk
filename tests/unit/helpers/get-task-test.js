import { getTask } from 'ember-gvk/helpers/get-task';
import { module, test } from 'qunit';

module('Unit | Helper | get task');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getTask([42]);
  assert.ok(result);
});
