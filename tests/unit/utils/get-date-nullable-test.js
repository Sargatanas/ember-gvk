import getDateNullable from 'ember-gvk/utils/get-date-nullable';
import { module, test } from 'qunit';

module('Unit | Utility | get date nullable');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getDateNullable();
  assert.ok(result);
});
