import dateString from 'ember-gvk/utils/date-string';
import { module, test } from 'qunit';

module('Unit | Utility | date string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateString();
  assert.ok(result);
});
