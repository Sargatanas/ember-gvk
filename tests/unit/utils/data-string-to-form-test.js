import dataStringToForm from 'ember-gvk/utils/data-string-to-form';
import { module, test } from 'qunit';

module('Unit | Utility | data string to form');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dataStringToForm();
  assert.ok(result);
});
