import taskCanBeDone from 'ember-gvk/utils/task-can-be-done';
import { module, test } from 'qunit';

module('Unit | Utility | task can be done');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = taskCanBeDone();
  assert.ok(result);
});
