import findTasks from 'ember-gvk/utils/find-tasks';
import { module, test } from 'qunit';

module('Unit | Utility | find tasks');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = findTasks();
  assert.ok(result);
});
