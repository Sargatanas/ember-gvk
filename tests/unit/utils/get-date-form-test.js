import getDateForm from 'ember-gvk/utils/get-date-form';
import { module, test } from 'qunit';

module('Unit | Utility | get date form');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getDateForm();
  assert.ok(result);
});
