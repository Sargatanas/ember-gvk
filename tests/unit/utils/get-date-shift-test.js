import getDateShift from 'ember-gvk/utils/get-date-shift';
import { module, test } from 'qunit';

module('Unit | Utility | get date shift');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getDateShift();
  assert.ok(result);
});
