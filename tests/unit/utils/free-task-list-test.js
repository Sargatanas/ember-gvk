import freeTaskList from 'ember-gvk/utils/free-task-list';
import { module, test } from 'qunit';

module('Unit | Utility | free task list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = freeTaskList();
  assert.ok(result);
});
